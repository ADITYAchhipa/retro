import '../../models/listing.dart';

/// Factory class for creating listing view models
/// This is a placeholder for compatibility - not used in the simplified cached version
class ListingViewModelFactory {
  // Placeholder methods for compatibility
  static dynamic fromProperty(dynamic property) {
    // Not used in the simplified implementation
    return property;
  }

  static dynamic fromVehicle(dynamic vehicle) {
    // Not used in the simplified implementation
    return vehicle;
  }
}
